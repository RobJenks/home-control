package org.rj.homectl.common.model.util;

public enum CoordSpace {
    Absolute,
    Relative        // Relative to the parent container; generally used for device having position/size relative to its parent room
}
